# LLMF_PW.yaml
actions: "enqueue, allocate, backfill"
tiers:
  - plugins:
      - name: priority
      - name: conformance
  - plugins:
      - name: predicates
      - name: nodeorder
        arguments:
          llmf.weight: 1
