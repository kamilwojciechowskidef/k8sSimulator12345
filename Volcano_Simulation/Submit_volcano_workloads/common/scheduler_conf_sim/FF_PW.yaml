# FF_PW.yaml
actions: "enqueue, allocate, backfill"
tiers:
  - plugins:
      - name: priority
      - name: conformance
  - plugins:
      - name: predicates
      - name: ff
        arguments:
          ff.weight: 1
