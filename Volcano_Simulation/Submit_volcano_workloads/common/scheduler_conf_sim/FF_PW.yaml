# FF_PW.yaml
actions: "enqueue, allocate, backfill"
tiers:
  - plugins:
      - name: priority
      - name: conformance
  - plugins:
      - name: predicates
      - name: nodeorder
        arguments:
          ff.weight: 1
